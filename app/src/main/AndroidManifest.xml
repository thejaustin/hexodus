<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for deep system theming (granted via Shizuku) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.DUMP" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    
    <!-- Network permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Samsung-specific permissions -->
    <uses-permission android:name="com.samsung.android.providers.context.permission.WRITE_USE_APP_FEATURE_SURVEY" />
    <uses-permission android:name="com.sec.android.provider.badge.permission.WRITE" />
    <uses-permission android:name="com.sec.android.provider.badge.permission.READ" />

    <application
        android:name=".HexodusApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hexodus"
        android:resizeableActivity="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hexodus"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Overlay Activation Service -->

        <!-- Material You Override Service -->

        <!-- Foldable Display Listener Service -->

        <!-- High Contrast Injector Service -->

        <!-- Theme Manager Service -->

        <!-- Sentry crash reporting -->
        <meta-data android:name="io.sentry.dsn" android:value="https://befafa4507a58a7e9d2e8eacc380c7bd@o4510887187841024.ingest.us.sentry.io/4510970837532672" />
        <meta-data android:name="io.sentry.environment" android:value="${sentryEnvironment}" />
        <meta-data android:name="io.sentry.release" android:value="${sentryRelease}" />

        <!-- ANR detection: report when main thread is blocked >5s -->
        <meta-data android:name="io.sentry.anr.enable" android:value="true" />
        <meta-data android:name="io.sentry.anr.timeout-interval-millis" android:value="5000" />

        <!-- Performance tracing: capture 20% of sessions -->
        <meta-data android:name="io.sentry.traces-sample-rate" android:value="0.2" />

        <!-- Profiling: CPU profiling on 10% of traced sessions -->
        <meta-data android:name="io.sentry.profiles-sample-rate" android:value="0.1" />

        <!-- Session tracking: release health / crash-free rate dashboard -->
        <meta-data android:name="io.sentry.session-tracking.enable" android:value="true" />

        <!-- Breadcrumbs: trail of events leading up to a crash -->
        <meta-data android:name="io.sentry.breadcrumbs.user-interaction" android:value="true" />
        <meta-data android:name="io.sentry.breadcrumbs.system-events" android:value="true" />
        <meta-data android:name="io.sentry.breadcrumbs.app-components" android:value="true" />
        <meta-data android:name="io.sentry.breadcrumbs.network-events" android:value="true" />

        <!-- Privacy: never send PII (IPs, emails, etc.) -->
        <meta-data android:name="io.sentry.send-default-pii" android:value="false" />

        <!-- Screenshots disabled — app handles sensitive system UI -->
        <meta-data android:name="io.sentry.attach-screenshots" android:value="false" />

        <!-- Shizuku Provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL">
            <meta-data
                android:name="moe.shizuku.client.V3_SUPPORT"
                android:value="true" />
        </provider>

        <!-- System Tuner Service -->

        <!-- App Themer Service -->

        <!-- Gesture Manager Service -->

        <!-- Media Notification Service -->

        <!-- Audio Manager Service -->

        <!-- App Manager Service -->

        <!-- Privacy Security Service -->

        <!-- Network Firewall Service -->

        <!-- Power Manager Service -->

        <!-- Device Specific Service -->

        <!-- Advanced Feature Service -->

        <!-- Advanced Overlay Service -->

        <!-- Advanced Theming Service -->

        <!-- Automation Service -->

        <!-- Backup Restore Service -->

        <!-- Font Icon Manager Service -->

        <!-- Performance Optimizer Service -->

        <!-- Privacy Manager Service -->

        <!-- Resource Manager Service -->

        <!-- System Inspector Service -->

        <!-- Wallpaper Themer Service -->

        <!-- Dynamic Color Service -->

        <!-- Accessibility Checker Service -->

        <!-- Security Validation Service -->
        
        <!-- Feature Flags Service -->

        <!-- Shizuku Installer Service -->

        <!-- File Provider for APK Installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>